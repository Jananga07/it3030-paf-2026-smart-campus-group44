package backend_paf.Module3.config;

// M3 mock user seeding disabled — authentication is handled by Module 5 (AppUser / AppUserRepository).
// The old users_m3 table is no longer used.
public class DataInitializer {
}
