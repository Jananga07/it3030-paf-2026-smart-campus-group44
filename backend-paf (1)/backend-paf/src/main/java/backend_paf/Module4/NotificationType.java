package backend_paf.Module4;

/**
 * Enum representing the type of notification.
 * Extend this enum as new module events are added.
 */
public enum NotificationType {
    BOOKING_APPROVED,
    BOOKING_REJECTED,
    TICKET_STATUS_CHANGED,
    NEW_TICKET_COMMENT
}
